- name: Get package name from package.json
  id: pkg
  run: echo "name=$(jq -r .name package.json)" >> "$GITHUB_OUTPUT"

- name: Archive dist folder
  run: tar -czf ${{ steps.pkg.outputs.name }}.tgz dist/
